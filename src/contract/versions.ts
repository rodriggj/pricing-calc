// Contract Module — pinned schema and renderer version constants.
//
// Both constants are stamped into the metadata block at the bottom of every
// rendered Markdown document so historical artifacts remain interpretable.

/** Schema version for the contract wire payload format. */
export const CONTRACT_SCHEMA_VERSION = 'v1.0.0';

/** Renderer version for the Markdown projection functions. */
export const RENDERER_VERSION = 'v1.0.0';
